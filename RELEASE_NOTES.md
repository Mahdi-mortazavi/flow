# Flow (تک‌نقطه) ۰.۵.۱ — رفع کرش استارت روی نسخه‌ی نصبی

این یک نسخه‌ی اصلاحی مهم است. نسخه‌ی نصبی (release) روی برخی گوشی‌ها — به‌ویژه دستگاه‌های با GPU مالی مثل گلکسی A30s و اندروید ۱۱ — **هنگام باز شدن صفحه‌سیاه می‌شد و بالا نمی‌آمد.** حالا کامل حل شده.

## 📥 نصب و تست

1. فایل `taknoghte-v0.5.1-arm64-v8a.apk` را از پایین همین صفحه دانلود کنید.
2. اگر نسخه‌ی قبلی نصب است، **اول آن را پاک کنید** (نسخه‌ی قبلی خراب بود)، بعد این را نصب کنید.
3. اجازه‌ی «نصب از منابع ناشناس» را بدهید و اجازه‌ی نوتیفیکیشن را در اولین اجرا تأیید کنید.

> **پیش‌نیاز:** اندروید ۷+، پردازنده‌ی ۶۴بیتی (arm64-v8a). فایل `‎.sha256` برای راستی‌آزمایی ضمیمه است.

---

## 🐛 چه چیزی درست شد

**کرشِ صفحه‌سیاه هنگام استارت (بحرانی).** دو مشکل که روی هم این باگ را می‌ساختند:

1. **آیکونِ نوتیفیکیشن در بیلد release حذف می‌شد.** آیکون `ic_stat_dot` فقط از کد (زمان اجرا) صدا زده می‌شد، برای همین بهینه‌سازِ منابعِ اندروید آن را «بی‌استفاده» تشخیص می‌داد و از نسخه‌ی نهایی حذف می‌کرد. بعد هنگام راه‌اندازیِ نوتیفیکیشن، خطای «آیکون پیدا نشد» رخ می‌داد.
   - آیکون حالا به‌صورت PNG در همه‌ی چگالی‌ها ساخته شده و صریحاً در بیلد نگه داشته می‌شود؛ بهینه‌سازِ حذفِ منابع و کد هم برای پایداری خاموش شد.

2. **راه‌اندازیِ نوتیفیکیشن، جلوی بالا آمدنِ کل اپ را می‌گرفت.** اگر راه‌اندازی نوتیفیکیشن به هر دلیلی شکست می‌خورد (آیکون، دسترسی، یا محدودیت سازنده)، چون پیش از رندرِ اپ و بدون مدیریتِ خطا اجرا می‌شد، **کل برنامه بالا نمی‌آمد.**
   - حالا اپ همیشه بالا می‌آید؛ راه‌اندازیِ نوتیفیکیشن با مدیریتِ کاملِ خطا و بدون بلاک‌کردنِ استارت انجام می‌شود. هیچ خطای جانبی دیگر نمی‌تواند اپ را زمین بزند.

## ✅ تأیید روی دستگاه واقعی

روی **Samsung Galaxy A30s (Mali-G71، اندروید ۱۱)** آزمایش شد: نسخه‌ی release پیش از این صفحه‌سیاه می‌شد؛ حالا آنبردینگ، ویزارد صبح و راه‌اندازیِ نوتیفیکیشن‌ها بدون هیچ خطا اجرا می‌شوند.

## 🔒 حریم خصوصی

مثل همیشه: همه‌چیز فقط روی گوشی خودتان. بدون اکانت، بدون سرور، بدون ردیابی.

## 🧪 کیفیت

۵۰ تست خودکار + تحلیل استاتیک سخت‌گیرانه + بررسی فرمت در CI.
