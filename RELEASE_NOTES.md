# Flow (تک‌نقطه) ۰.۵.۰ — «سریع‌تر، هوشمندتر، چسبنده‌تر»

این نسخه سراغِ شکاف‌های پروداکتی رفته — همان چیزهای کوچکی که فاصله‌ی «یک اپِ خوب» تا «اپی که واقعاً رفتارت را عوض می‌کند» را می‌سازند.

## 📥 نصب و تست

1. فایل `taknoghte-v0.5.0-arm64-v8a.apk` را از پایین همین صفحه دانلود کنید.
2. اجازه‌ی «نصب از منابع ناشناس» را بدهید و نصب کنید.
3. در اولین اجرا اجازه‌ی نوتیفیکیشن (و در اندروید ۱۲+ «زنگ دقیق») را تأیید کنید.

> **پیش‌نیاز:** اندروید ۵٫۰+، پردازنده‌ی ۶۴بیتی (arm64-v8a). فایل `‎.sha256` برای راستی‌آزمایی ضمیمه است.

---

## ✏️ ویرایش و حذفِ کار — بدون چیدنِ دوباره
- **لمسِ طولانی** روی هر کار (یا تخته‌سنگ) → شیت ویرایش/حذف
- غلط تایپی؟ در دو ثانیه درستش کن — دیگر لازم نیست کل روز را replan کنی
- عنوان هم‌زمان در برنامهٔ امروز و در مخزنِ کارها اصلاح می‌شود
- حذفِ تخته‌سنگ، تاجِ «مهم‌ترین کار» را به کارِ بعدی می‌سپارد

## 📱 قطعِ تمرکز، حالا یک ضربه است
- به‌جای تایپِ دلیل، **چیپ‌های یک‌لمسی**: 📱 موبایل · 👥 آدم‌ها · 😴 خستگی · 💭 فکر مزاحم · ✍️ دیگر (+ یک خطِ اختیاری)
- در **آینه**، الگوی قطع‌شدن‌ها دیگر فهرستِ یادداشت نیست؛ **نمودارِ رتبه‌بندی‌شده‌ی ۳۰ روز** است — این تازه می‌شود «الگو» و می‌فهمی دشمنِ اصلیِ تمرکزت کیست

## 🎮 وقتِ آزاد، حالا پاداش است نه حواس‌پرتی
- تا وقتی تخته‌سنگ نیفتاده، دکمهٔ شروعِ تفریح **قفلِ نرم** دارد: «تخته‌سنگ بیفتد، بی‌گناه‌تر می‌شود» — با راه فرارِ آگاهانه
- بعد از افتادنِ تخته‌سنگ، خودش باز می‌شود. پاداشِ شرطی = چسبِ رفتاریِ قوی (temptation bundling)

## 🎯 آمار، حالا صادق‌تر است
- **شکاف خوش‌بینی** فقط بعد از ۵ شبِ بسته نشان داده می‌شود؛ قبل از آن با یک شبِ بدشانس، عددِ ترسناک و بی‌معنا نمی‌سازد
- قبل از آن، پیام: «چند شب دیگر تا معنا پیدا کردن»

## ⚡ میان‌بُرِ «ثبت فکر»
- **لمسِ طولانی روی آیکونِ اپ** در لانچر → مستقیم «ثبت فکر» باز می‌شود، بدون گشتنِ کاملِ اپ. تخلیهٔ ذهن، حالا از هرجای گوشی

## 🩹 زیرِ پوسته
- دیتابیس نسخهٔ ۳ با مهاجرتِ امن: ستونِ دسته‌بندیِ قطع‌ها + ایندکس‌های سرعت روی جدول‌های تاریخی
- ۱۱ تستِ جدید (مجموع ۴۹) + تحلیل استاتیک سخت‌گیرانه + بررسی فرمت در CI

## 🔒 حریم خصوصی

مثل همیشه: همه‌چیز فقط روی گوشی خودتان. بدون اکانت، بدون سرور، بدون ردیابی.
