# Flow (تک‌نقطه) ۰.۵.۲ — نصب روی همه‌ی گوشی‌ها

اگر هنگام نصب پیام **«بسته مشکل دارد» / «There was a problem parsing the package»** می‌گرفتی، این نسخه دقیقاً همان را حل می‌کند.

## 📥 کدام فایل را دانلود کنم؟

| فایل | برای چه کسی |
|---|---|
| **`taknoghte-v0.5.2-universal.apk`** ⭐ | **اگر شک داری، همین را بگیر.** روی همه‌ی گوشی‌ها نصب می‌شود. |
| `taknoghte-v0.5.2-arm64-v8a.apk` | گوشی‌های ۶۴بیتی (بیشتر گوشی‌های ۲۰۱۷ به بعد) — حجم کمتر |
| `taknoghte-v0.5.2-armeabi-v7a.apk` | گوشی‌های ۳۲بیتی و قدیمی‌تر |
| `taknoghte-v0.5.2-x86_64.apk` | شبیه‌ساز و دستگاه‌های اینتل |

۱. فایل را دانلود کن. ۲. اجازه‌ی «نصب از منابع ناشناس» را بده. ۳. نصب کن.

> **پیش‌نیاز:** اندروید ۷ (API 24) به بالا. برای هر فایل یک `‎.sha256` هست تا از سالم بودن دانلود مطمئن شوی.

---

## 🐛 چرا نصب نمی‌شد

سه علت روی هم جمع شده بودند:

**۱. فقط نسخه‌ی ۶۴بیتی منتشر می‌شد.** ریلیزهای قبلی تنها `arm64-v8a` داشتند. هر گوشیِ ۳۲بیتی (armeabi-v7a) که آن فایل را دانلود می‌کرد، اندروید بسته را ناسازگار می‌دید و همان خطای «مشکل در تجزیه‌ی بسته» را می‌داد. **حالا نسخه‌ی یونیورسال منتشر می‌شود که هر سه معماری را با هم دارد** و همیشه نصب می‌شود.

**۲. امضا با کلیدِ دیباگ.** بیلد release با کلید debug امضا می‌شد. این کلید روی هر اجرای CI از نو ساخته می‌شود، یعنی **هر نسخه با کلیدی متفاوت امضا می‌شد** — به همین دلیل به‌روزرسانی روی نصب قبلی با خطای عدم تطابق امضا شکست می‌خورد و بعضی نصب‌کننده‌های سازنده‌ها کلاً بسته را رد می‌کردند. حالا امضای واقعی release با هر سه اسکیم v1+v2+v3 انجام می‌شود (v1 برای نصب‌کننده‌های قدیمی‌تر لازم است).

**۳. نسخه‌ی حداقلی نامشخص بود.** مقادیر SDK از پیش‌فرض‌های Flutter می‌آمدند و با هر ارتقای SDK می‌توانستند بی‌صدا عوض شوند و سازگاری دستگاه‌ها را تغییر دهند. حالا صریح پین شده‌اند: **minSdk 24، targetSdk 36، compileSdk 36**.

## 🛡 از این به بعد چنین چیزی منتشر نمی‌شود

خط تولید حالا قبل از انتشار، هر فایل APK را خودکار بازرسی می‌کند و اگر هر کدام رد شود، انتشار متوقف می‌شود:

- سلامت ساختار فایل (zip سالم)
- خواندن مانیفست و بررسی `minSdk`
- اطمینان از اینکه نسخه‌ی یونیورسال واقعاً **هر سه معماری** را دارد
- صحت امضا و کامل بودن اسکیم‌های v1/v2/v3
- **رد کردن انتشار اگر بسته با کلید دیباگ امضا شده باشد**
- ساخت `SHA256` برای همه‌ی فایل‌ها

## 🔒 حریم خصوصی

مثل همیشه: همه‌چیز فقط روی گوشی خودت. بدون اکانت، بدون سرور، بدون ردیابی.

## 🧪 کیفیت

۵۰ تست خودکار + تحلیل استاتیک سخت‌گیرانه + بررسی فرمت + بازرسی APK، همه در CI.
