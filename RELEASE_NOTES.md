# Flow (تک‌نقطه) ۰.۴.۱ — نسخه‌ی «قابل اتکا»

> نکته: نسخه‌ی ۰.۴.۰ به‌دلیل خطای فرایند انتشار، از کد قدیمی ساخته شده بود و معتبر نیست — این نسخه (۰.۴.۱) ریلیز صحیح همان تغییرات است.

این نسخه حاصل یک ممیزی کامل پروداکت است: **۱۵ مشکل شناسایی‌شده — از حلقه‌ی بازگشت کاربر تا دسترسی‌پذیری — همگی در همین نسخه برطرف شدند.** هیچ فیچر نمایشی‌ای اضافه نشده؛ همه‌چیز برای این است که اپ هر روز، بی‌صدا و بی‌خطا، کارش را بکند.

## 📥 نصب و تست

1. فایل `taknoghte-v0.4.1-arm64-v8a.apk` را از پایین همین صفحه دانلود کنید.
2. اجازه‌ی «نصب از منابع ناشناس» را بدهید و نصب کنید.
3. در اولین اجرا اجازه‌ی نوتیفیکیشن (و در اندروید ۱۲+ «زنگ دقیق») را تأیید کنید.

> **پیش‌نیاز:** اندروید ۵٫۰+، پردازنده‌ی ۶۴بیتی (arm64-v8a). فایل `‎.sha256` برای راستی‌آزمایی ضمیمه است.

---

## 🔄 حلقه‌ی روزانه حالا خودش زنده می‌ماند

- **یادآور صبح:** اگر روز هنوز چیده نشده باشد — «سه کار، یک تخته‌سنگ، یک پیش‌بینی»
- **یادآور شب:** اگر روز هنوز بسته نشده باشد — «۶۰ ثانیه: چک، چرا، یک خط»
- هر دو **هوشمندند**: روزی که چیده/بسته شده باشد، بی‌صدا رد می‌شوند
- **یادآور هفتگی آینه** (جمعه‌شب‌ها) برای نگاه به اعداد هفته
- ساعت‌ها را همان **آنبردینگ** می‌پرسد و بعداً از تنظیمات (آیکون ⚙ بالای صفحه) قابل تغییر یا خاموش‌کردن است
- **رول‌اور نیمه‌شب:** اپ باز هم بماند، رأس تغییر روز، صفحه نو می‌شود و ویزارد صبح می‌آید

## 🎨 هویت بصری روی خود گوشی

- **آیکون جدید اپ:** نقطه‌ی کهربایی درخشان روی زمینه‌ی تقریباً سیاه (+ آیکون adaptive اندروید)
- **آیکون نوتیفیکیشن مونوکروم اختصاصی** — دیگر مربع خاکستری بی‌شکل در نوار وضعیت نیست؛ همه‌ی اعلان‌ها با رنگ کهربایی برند

## 💾 داده‌ی تو، دست تو

- **پشتیبان‌گیری کامل JSON** با یک ضربه — از طریق اشتراک‌گذاری به هر جا (فایل، تلگرام، درایو)
- **بازیابی از فایل** با تأیید صریح + اعتبارسنجی فایل (فایل غریبه رد می‌شود)
- **Undo برای حذف‌ها:** حذف فکر در مخزن ذهن حالا ۵ ثانیه دکمه‌ی «برگردان» دارد
- **بازبینی مبنا-صفر ایمن شد:** حذف‌ها فقط در پایان بازبینی اعمال می‌شوند؛ رها کردن وسط کار، هیچ چیز را پاک نمی‌کند

## 🐛 باگ‌های رفع‌شده

- «ارتقا به کار» دیگر فکر را به روزِ بسته‌شده اضافه نمی‌کند (به backlog فردا می‌رود)
- جلسه‌ی تمرکزی که وقتش در غیاب شما تمام شود، حالا در **زمان واقعی پایان** در آمار ثبت می‌شود — نه اینکه برای همیشه «باز» بماند
- انتخاب «هنوز نه» در پایان تایمر دیگر جلسه را «موفق» ثبت نمی‌کند (داده‌ی آمار تمیز ماند)
- ثبت مجدد پایان جلسه، یادداشتِ دلیل قطع را دیگر پاک نمی‌کند

## ⏰ اعتماد به زنگ‌ها

- در اندروید ۱۲+ اجازه‌ی **زنگ دقیق** هنگام راه‌اندازی درخواست می‌شود
- اگر داده نشود، زیر تایمر شفاف نوشته می‌شود: «زنگ پایان ممکن است کمی دیر برسد»
- **راهنمای باتری** در تنظیمات برای گوشی‌هایی که نوتیفیکیشن‌ها را می‌کشند (شیائومی، هواوی و…) — با دکمه‌ی مستقیم به تنظیمات سیستم
- اکشن «انجام شد ✓» روی یادآور عادت حالا برای iOS هم پیکربندی شده

## ♿ دسترسی‌پذیری

- همه‌ی کنترل‌های لمسی به حداقل **۴۴px** رسیدند
- کنتراست متن‌های اطلاعاتی بالا رفت (٪۳۰ ← ٪۳۸)
- برچسب‌های Semantics برای اسکرین‌ریدر روی کنترل‌های آیکونی
- پشتیبانی از فونت درشت سیستم تا ۱.۳× بدون به‌هم‌ریختگی
- محافظ لمس دور دکمه‌های تخته‌سنگ — لمس اشتباهی نزدیک دکمه، دیگر تمرکز را ناخواسته شروع نمی‌کند

## 🩹 سایر

- خطاهای احتمالی دیتابیس حالا صفحه‌ی انسانی با دکمه‌ی «تلاش دوباره» دارند، نه متن خام خطا

## 🔒 حریم خصوصی

مثل همیشه: همه‌چیز فقط روی گوشی خودتان. بدون اکانت، بدون سرور، بدون ردیابی.

## 🧪 کیفیت

۴۳ تست خودکار (۶ تست جدید برای همین اصلاح‌ها) + تحلیل استاتیک سخت‌گیرانه + بررسی فرمت، در هر کامیت روی CI.
